s#! /بن/ش الأمر -v git-lfs >/dev/null 2>&1 || { echo >&2 "\nتم تكوين هذا المستودع ل Git LFS ولكن لم يتم العثور على "git-lfs" على طريقك. إذا لم تعد ترغب في است
خدام Git LFS، فقم بإزالة هذا الخطاف عن طريق حذف ملف "ما بعد الالتزام" في دليل الخطافات (الذي تم تعيينه بواسطة "core.hookspath"؛ عادة '.git/hooks').\n"؛ exit 2؛ } git lfs post-commit "$@"
#!/bin/sh
command -v git-lfs >/dev/null 2>&1 || { echo >&2 "\nThis repository is configured for Git LFS but 'git-lfs' was not found on your path. If you no longer wish to use Git LFS, remove this hook by deleting the 'post-commit' file in the hooks directory (set by 'core.hookspath'; usually '.git/hooks').\n"; exit 2; }
git lfs post-commit "$@"
asd
